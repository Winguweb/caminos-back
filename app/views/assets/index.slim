section
  aside
    div
      section
        nav
          div =  "Acá podes encontrar y agregar lugares claves dentro del barrio que brindan apoyo o servicios indispensables en el contexto de emergencia por COVID-19"

          - if @assets.present?
            == cell('elements/features_tree', @assets)

          - else
            .empty-state 
              span = t('.no_data')

  main.page-section
    section
      button.floating-btn type="button"
        span.floating-btn-text
          = link_to t('.add_point'), new_neighborhood_asset_path(@neighborhood)
      == cell('elements/map_show', @neighborhood, features: @assets)
