.map-show-cell
  #map-container

javascript:
  $(function() {
    new CDLV.Components['map_show']({
      el: $('.map-show-cell'),
      defaults: #{MAP.to_json},
      token: '#{MAPBOX_TOKEN}',
      base: #{base},
      features: #{features},
    })
  });
